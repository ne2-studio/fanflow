# Local development environment: Postgres + both services, each built from its own Dockerfile.
#
# The frontend's Dockerfile builds the Vite app itself (multi-stage: node build -> nginx runtime).
# VITE_API_URL/VITE_OIDC_* are baked in at build time via build args (defaulted below to match the
# backend port and the fake-oidc service mapped in this file); override them with a frontend/.env
# file (see frontend/.env.example) if you need different values.
#
# Local-only auth: `fake-oidc` replaces the real OIDC provider so you can log in without external
# credentials. It always logs you in as the "admin" user (no login screen). This is dev-only —
# staging/production keep using the real provider, configured via appsettings.Production.json and
# deploy-time CI vars, untouched by this file.
#
# One-time setup: the browser and the backend container both need to resolve the hostname
# "fake-oidc" (its issuer URL is baked into the tokens it issues, so it must be the same host from
# everywhere). Add this line to your hosts file (WSL2/Linux/macOS: /etc/hosts; Windows browser
# hitting a WSL2 Docker daemon: C:\Windows\System32\drivers\etc\hosts):
#   127.0.0.1 fake-oidc

services:
  fake-oidc:
    image: ghcr.io/ne2-studio/fake-oidc:latest
    ports:
      - "5000:5000"
    environment:
      OIDC_ISSUER: "http://fake-oidc:5000"
      OIDC_GENERATE_KEY: "true"
      OIDC_DEFAULT_USER: "admin"
      OIDC_TOKEN_LIFETIME_SECONDS: "3600"
      OIDC_CLIENTS: >-
        [{"clientId":"frontend","redirectUris":["http://localhost:3000/callback"]}]
      OIDC_USERS: >-
        [{"key":"admin","sub":"admin-user","email":"admin@test.local","name":"Admin User","roles":["admin"]},
        {"key":"user","sub":"normal-user","email":"user@test.local","name":"Normal User","roles":["user"]}]

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: fanflow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U devuser -d fanflow"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=fanflow;Username=devuser;Password=devpass"
      Publishing__PublicHostname: "http://localhost:8081"
      Publishing__Minio__Endpoint: "http://minio:9000"
      Publishing__Minio__AccessKey: "minioadmin"
      Publishing__Minio__SecretKey: "minioadmin"
      Publishing__Minio__Bucket: "releases"
      Auth__Authority: "http://fake-oidc:5000"
      Auth__Audience: "frontend"
    ports:
      - "5050:8080"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      fake-oidc:
        condition: service_started

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:5050}
        VITE_OIDC_AUTHORITY: ${VITE_OIDC_AUTHORITY:-http://fake-oidc:5000}
        VITE_OIDC_CLIENT_ID: ${VITE_OIDC_CLIENT_ID:-frontend}
    ports:
      - "3000:80"
    depends_on:
      - backend

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Internet-facing static site container: serves published release landing pages straight from
  # MinIO and proxies the tracking beacons (/pv, /out, /trap) to the backend. This is what
  # Publishing:PublicHostname (above) points at in production.
  site:
    build:
      context: ./site
      dockerfile: Dockerfile
    environment:
      MINIO_ENDPOINT: "minio:9000"
      MINIO_BUCKET: "releases"
      BACKEND_HOST: "backend:8080"
    ports:
      - "8081:80"
    depends_on:
      minio:
        condition: service_healthy
      backend:
        condition: service_started

volumes:
  pgdata:
  miniodata:
